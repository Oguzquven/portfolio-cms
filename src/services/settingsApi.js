const URL="/api/settings";async function request(options={}){const response=await fetch(URL,{headers:options.body?{"Content-Type":"application/json"}:undefined,...options});if(!response.ok){const body=await response.json().catch(()=>null);throw new Error(body?.message||`İstek başarısız oldu (${response.status})`);}return response.json();}export const getSettings=()=>request();export const updateSettings=value=>request({method:"PUT",body:JSON.stringify(value)});
